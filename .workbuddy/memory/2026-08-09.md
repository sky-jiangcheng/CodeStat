# 2026-08-09

## 解决 PR #58（a11y）与 master 的冲突

- 仓库已由 `sky-jiangcheng/gitboard` 重命名为 `sky-jiangcheng/gitbuddy`，本地 origin 已更新为新地址。
- 本地 master 从 bbe5a2e 快进到 1187237（落后 39 个提交）。
- PR #58 分支 `260808-feat-a11y`（head 24c6faf，base 落后于 master）冲突文件 4 个：
  App.tsx / CommandPalette.tsx / Dashboard.tsx / Settings.tsx。
- 关键结论：master 在 #56 合并后**已包含等价甚至更完善的无障碍实现**，
  因此冲突解决以 master 为准，仅叠加 PR 独有增量。
  - App.tsx：保留 master 的 `<header>` + skip-link(`#main-content`) 结构，
    追加 `role="search"` 地标与搜索按钮 `aria-haspopup="dialog"`。
  - CommandPalette.tsx：整体采用 master 版（动态 aria-expanded、独立 visually-hidden
    live region、选项 scrollIntoView 同步），优于 PR 版。
  - Dashboard/Settings：统一 master 的 label/id 命名（dashboard-sort、settings-*）。
  - 去重修复：hero-title 从 h1 回退为 div（避免双 h1）；移除扫描按钮多余 aria-live；
    删除 global.css 末尾重复追加的无障碍样式块（含未使用的 .app-header）。
- 合并提交 d04d0e5 已推送到 `origin/260808-feat-a11y`，PR #58 状态变为 mergeable=true / clean，
  净差异收敛为 App.tsx + StatusBar.tsx 两个文件。
- 验证方式：`cd web && npm ci && npm run build`（tsc + vite build）通过。
- 环境备注：sandbox 下 `git fetch/push` 会被静默 kill（exit 137），需在非沙箱模式执行；
  本机未安装 `gh`，PR 信息通过 GitHub REST API + curl 获取。
